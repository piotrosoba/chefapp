import React from 'react'
import { Button } from '@material-ui/core'

const App = props => {
  return (
    <div>
      <Button
        color='primary'
        variant='contained'
      >
        OK
      </Button>
    </div>
  )
}

export default App